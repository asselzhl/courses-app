import { Login } from './components/Login/Login';

export function App() {
	
	return (
		<>
		<Login />
			{/* {localStorage.getItem('userToken') ? (
				<Courses />
			) : (
				<Login />
			)} */}
		</>
	);
}
